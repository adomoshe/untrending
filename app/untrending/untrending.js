
module.exports = article => {
    console.log(article)
    const { source, title } = article;
    const sourceId = source.id;
}

// Take out vertical less than 40 and horizontal less than -22 or greater than 22